import { initializePillowDreamworkGame } from '../lib/engine';

// Example usage: initialize the game engine
const gameModules = initializePillowDreamworkGame();

// Export for use in React components or external tools
export default gameModules;
