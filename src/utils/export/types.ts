
/**
 * Common types for exporting quantum data
 */

// Format for exporting quantum states
export interface ExportedQuantumState {
  stateName: string;
  probability: number;
  timestamp: string;
}

